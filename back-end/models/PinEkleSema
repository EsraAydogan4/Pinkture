const mongoose = require('mongoose');

const resimSchema = new mongoose.Schema({
  url: {
    type: String,
    required: true
  },
  tarih: {
    type: Date,
    default: Date.now
  }
});

module.exports = mongoose.model('Resim', resimSchema);
